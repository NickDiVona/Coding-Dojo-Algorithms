//  Given an array and an index, remove and return the array value at the given index.
//  Do not use built-in methods except

const someArray = [1, 3, 5, 7, 9];

function removeAt(arr, idx) {}

removeAt(someArray, 2);
