// create a function that converts temperature in C to F.

function celsiusToFahrenheit(cDegrees) {
  let fDegrees = cDegrees * 1.8 + 32;
  return fDegrees;
}

console.log(celsiusToFahrenheit(30));

// 0c == 32f
