function firstPlusLength(someArr) {
    // console.log(arr[0] + arr.length);
    return (arr[0] + arr.length);
}
var arr = [1,2,3,4];
firstPlusLength(arr);

/*
    Line 1:
        Set up function
        Give it a parameter of 'someArr'

    Line 2-3:
        Print first value of 'arr' + length of 'arr' if needed
        Return first value of 'arr' + length of 'arr'

    Line 5:
        Create variable 'arr'
        Give value of '[1,2,3,4]'

    Line 6:
        Call the function
        Pass in 'arr'
*/
